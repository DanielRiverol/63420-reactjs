import React from "react";

function InputCustom({ type = "text", value, placeholder }) {
  return;
  <div>
    <label htmlFor=''></label>//completar
    <input type={type} placeholder={placeholder} vlaue={value} />;
  </div>;
}

export default InputCustom;
